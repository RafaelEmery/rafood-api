apiVersion: batch/v1
kind: Job
metadata:
  name: rafood-migrations
  namespace: rafood
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded

spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrations
          image: rafood-api:latest
          imagePullPolicy: Never
          command: ["alembic", "upgrade", "head"]
