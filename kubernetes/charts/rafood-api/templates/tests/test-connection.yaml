apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rafood-api.fullname" . }}-test-connection"
  labels:
    {{- include "rafood-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rafood-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
